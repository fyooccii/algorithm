const strArr = ['B3', 'D2', 'F1', 'A9', 'D12', 'A2', 'C1', 'Z0', 'B1'];

const processArr = arr => {
    // const copyArr = arr.reduce((total, cur) => {
    //     const splitArr = cur.split('');
    //     return [...total, [splitArr[0], splitArr[1]]];
    // }, []);
};

processArr(strArr);
